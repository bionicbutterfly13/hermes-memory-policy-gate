# Hermes Memory Policy Gate installed

This plugin is shadow-mode only. It classifies proposed memory writes and returns auditable routing decisions. It does not mutate Hermes memory, Mnemosyne, skills, credentials, providers, gateway, or config.

Enable it with:

```bash
hermes plugins enable hermes-memory-policy-gate
```

Then start a fresh Hermes session so plugin tools are discovered.
